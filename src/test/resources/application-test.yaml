SKATT_URL: http://localhost:${wiremock.server.port:8097}
SAK_URL: http://localhost:${wiremock.server.port:8098}
AZURE_OPENID_CONFIG_ISSUER: http://localhost:${mock-oauth2-server.port}/aad
TOPIC_VEDTAK: bidrag.vedtak-feature-test

scheduler:
  sendkrav:
    cron: "-" #Disabled
  avstemning:
    cron: "-" #Disabled
  påløpkjøring:
    cron: "-" #Disabled

atlas.kafka.zookeeper.session.timeout.ms: 60000
atlas.kafka.zookeeper.connection.timeout.ms: 30000

spring:
  jpa:
    hibernate:
      ddl-auto: validate